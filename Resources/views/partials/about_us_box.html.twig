<!-- <div class="divider"></div> -->

<div class="{{ aboutUsWhiteBox ? 'content' : 'recentworks' }}">
    <div class="container">
        <div class="row">
            <div class="posts-block col-lg-8 col-md-8 col-sm-6 col-xs-12">
                <article class="about-us">
                    {% if not aboutUsHideTitle %}
                    <h3 class="title" id="quienes-somos">Qui&eacute;nes somos</h3>
                    {% endif %}
                    <div class="post-content">
                        {{ site.mainDescription | raw('html') }}
                    </div>
                </article>
            </div>

            <div class="col-lg-4 col-md-4 col-xs-12 col-sm-6">
                <div class="address widget">
                    <h3 class="title">{{ site.phone is not empty ? 'Llamanos ahora!' : 'Contactanos ahora!' }}</h3>
                    {% if site.phone is not empty %}
                    <p class="phone"><a href="tel:{{ site.phone }}" data-toggle="modal"><i class="icon-phone" style="margin-right: 8px;"></i> {{ site.phone }}</a></p>
                    {% endif %}

                    <p>
                        Pod&eacute;s hacernos un pedido o consultar por precios.
                        (Horario de atención de {{ site.open_hours is not empty ? site.open_hours : 'Lunes a Viernes de 9 a 18 hs' }})
                    </p>

                    <p class="action">
                        <a  class="btn btn-special btn-color" href="#contactModal" data-toggle="modal" onClick="_gaq.push(['_trackEvent', 'Landing', 'AskFreeEstimate', 'Button'])">
                            Solicitar Presupuesto Gratis
                        </a>
                    </p>
                </div>
            </div>

            <div class="clearfix"></div>
        </div>
    </div>
</div>

<div class="divider"></div>
